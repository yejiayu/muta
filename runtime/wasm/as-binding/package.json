{
  "name": "as-binding",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "asbuild:untouched": "asc assembly/index.ts -b build/untouched.wasm -t build/untouched.wat --validate --sourceMap --debug",
    "asbuild:optimized": "asc assembly/index.ts -b build/optimized.wasm -t build/optimized.wat --validate --sourceMap --optimize",
    "asbuild": "npm run asbuild:untouched && npm run asbuild:optimized",
    "asbuild:example": "asc examples/storage_service.ts -b build_examples/storage_service.wasm -t build_examples/storage_service.wat --validate --sourceMap --optimize"
  },
  "author": "yejiayu",
  "license": "ISC",
  "devDependencies": {
    "assemblyscript": "^0.9.4"
  },
  "dependencies": {
    "@assemblyscript/loader": "^0.9.4",
    "assemblyscript-json": "^0.3.0"
  }
}
